<h1>Power Supply Unit List</h1>
<div class="card-row">
  <% @psus.each do |c| %>
    <div class="card">
      <%=image_tag("#{c.url}", class: "card-image") %>

    <div class="container">
      <h4><b><%= link_to c.model, "/psus/#{c.id}" %></b></h4>
    </div>
  </div>
  <% end %>
</div>

<h3>Most Popular: <%= Psu.most_popular %></h3>
