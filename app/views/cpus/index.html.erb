<h1>CPU List</h1>
<div class="card-row">
  <% @cpus.each do |c| %>
    <div class="card">
      <%=image_tag("#{c.url}", class: "card-image") %>

    <div class="container">
      <h4><b><%= link_to c.model, "/cpus/#{c.id}" %></b></h4>
    </div>
  </div>
  <% end %>
</div>

<h3>Most Popular: <%= Cpu.most_popular %></h3>
